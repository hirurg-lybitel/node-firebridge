# Firebird Database Configuration
FIREBIRD_HOST=localhost
FIREBIRD_PORT=3050
FIREBIRD_DATABASE=/path/to/your/database.fdb
FIREBIRD_USER=SYSDBA
FIREBIRD_PASSWORD=masterkey
FIREBIRD_ROLE=
FIREBIRD_PAGE_SIZE=4096
FIREBIRD_LOWER_CASE_KEYS=false

# Server Configuration
PORT=3000
NODE_ENV=development

# Connection Pool Configuration
POOL_MIN=2
POOL_MAX=10

# Query Timeout (in milliseconds, default: 10000)
FIREBIRD_QUERY_TIMEOUT=10000

# Async Query Timeout (in milliseconds, default: 3600000)
FIREBIRD_ASYNC_QUERY_TIMEOUT=3600000

